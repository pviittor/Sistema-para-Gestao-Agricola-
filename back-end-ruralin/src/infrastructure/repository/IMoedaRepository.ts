import { IRepository } from '../../core/repository/IRepository';
import Moeda from '../../models/Moeda';

/**
 * Interface para repositório de Moeda
 */
export interface IMoedaRepository extends IRepository<Moeda> {
  // TODO: Adicionar métodos customizados se necessário
}
